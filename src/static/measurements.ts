// export const standard = "standard";
// export const metric = "metric";

enum Measurements {
  standard = "standard",
  metric = "metric",
}

export default Measurements;

// const measurements = {
// };

// // type Measurements = typeof measurements;

// type MeasurementType = keyof measurements;

// export { MeasurementType };

// export default measurements;
